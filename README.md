# Enterprise Monitor: Gulp-Flask Dashboard

## 📊 Project Overview

**Enterprise Monitor** is a responsive, containerized analytics dashboard allowing real-time data visualization. Built with **Flask** for the backend and **Gulp** for efficient frontend asset management, it provides a scalable boilerplate for monitoring applications.

## 🔑 Key Features

- **Automated Workflow**: Uses **Gulp** for SCSS compilation, JS minification, and live reloading.
- **Containerized Deployment**: Fully Dockerized application with `docker-compose` orchestration.
- **Dynamic Visualizations**: Integrated Chart.js for rendering complex datasets.
- **RESTful Backbone**: Python Flask backend serving API endpoints and rendering templates.

## 🛠️ Tech Stack & Skills

- **Backend**: Python, Flask
- **Frontend**: HTML5, SCSS, JavaScript, Gulp
- **DevOps**: Docker, Docker-Compose, Bash Scripting
- **Architecture**: MVC (Model-View-Controller)

## 💡 Innovation

This project demonstrates **DevOps competency** alongside full-stack development. By automating the build pipeline and containerizing the environment, it ensures consistent behavior across development, staging, and production—a critical skill for modern software reliability.

## 🚀 Setup

run via Docker:

```bash
docker-compose up --build
```
